The previously provided arguments to {{{retrieveCodeContextFunctionName}}} were invalid when we attempted to
extract code using it. Here is the feedback we received:

{{{feedback}}}

Hint: `file_path` may be either the file where a symbol is defined OR any file
that references/uses it. When the symbol isn't defined in `file_path`, the tool
resolves the real definition via LSP go-to-definition (possibly in another repo
file or a third-party library/stdlib). `reference_line` is only an optional
disambiguator. Omit it normally: without it, all occurrences are resolved and
the resulting definitions are de-duplicated. Set it only when you need to choose
among reference/usage occurrences that could resolve differently, and identify
the intended usage rather than a declaration such as `func Something`.