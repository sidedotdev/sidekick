; All class declarations (including value classes, enums, and regular classes)
(
  [
    (multiline_comment)
    (line_comment)
  ]* @doc
  .
  (class_declaration
    (type_identifier) @name
  )
  {{#SymbolName}}(#eq? @name "{{SymbolName}}"){{/SymbolName}}
) @definition

(
  [
    (multiline_comment)
    (line_comment)
  ]* @doc
  .
  (object_declaration
    (type_identifier) @name
  )
  {{#SymbolName}}(#eq? @name "{{SymbolName}}"){{/SymbolName}}
) @definition

; Function declarations
(
  [
    (multiline_comment)
    (line_comment)
  ]* @doc
  .
  (function_declaration
    (simple_identifier) @name
  )
  {{#SymbolName}}(#eq? @name "{{SymbolName}}"){{/SymbolName}}
) @definition

; enum members
(enum_entry
  (simple_identifier) @name
  {{#SymbolName}}(#eq? @name "{{SymbolName}}"){{/SymbolName}}
) @definition